/*@flow*/
'use strict';

import { AppRegistry } from 'react-native';
import RootContainer from './app/containers/root.container';

AppRegistry.registerComponent('lets_play_app', () => RootContainer);
